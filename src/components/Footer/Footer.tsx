import { FC } from "react";
import { Layout } from "antd";

const { Footer } = Layout;

export const AppFooter: FC = () => {
  return (
    <Footer className="footer">
      Happy watching movie collections!!!
    </Footer>
  );
};
